# Server Configuration
PORT=7518
NODE_ENV=production

# Email OTP Service Configuration (Resend API or Hostinger SMTP)
# Option A: Resend API (Recommended)
RESEND_API_KEY=
EMAIL_FROM="RencanaNgoding.ai" <otp@ksatriyo.id>

# Option B: Hostinger / Custom SMTP (Optional fallback)
# SMTP_HOST=smtp.hostinger.com
# SMTP_PORT=465
# SMTP_USER=otp@ksatriyo.id
# SMTP_PASS=your_email_password
